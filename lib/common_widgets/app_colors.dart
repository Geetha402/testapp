import 'package:flutter/material.dart';

abstract class AppColors {
 
 static const appbar = Color(0xFF047CE0);
 static const white = Colors.white;
 static const black = Color(0xFF293042);
 static const red = Colors.red;
 static const lightblue = Color(0xFFDCECF9);
 static const grey = Colors.grey;
 static const lightgrey = Color(0xFFD9D9D9);
 static const purple = Color(0xFF6504E0);
 static const buleshade = Color(0xFFE8EDF8);
 static const lightblueshade = Color(0xFF80B8DE);

}
